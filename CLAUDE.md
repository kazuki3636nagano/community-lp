# CLAUDE.md — And Family コミュニティLP

## このプロジェクトは何か

一般社団法人日本マタニティフード協会のコミュニティ「And Family」のLP。
**唯一のゴールは公式LINE(https://lin.ee/6G6ifwV)の友だち追加**。デザインの美しさより登録率を優先する。

- ターゲット: ママ・パパ・プレママ・プレパパ(妊活中〜育児後も歓迎)。流入の大半はスマホ(店舗QR・SNS・Google検索)
- 公開: GitHub Pages(mainブランチ直下から自動デプロイ。pushすれば1〜2分で反映)
- 関係者: 山本代表(最終判断)/ 谷口(マーケ室CD・デザイン承認)/ 園部(コミュニティ運用)

## 構成

```
index.html   … LP本体。HTML/CSS/JS すべてこの1ファイルに完結
images/      … 写真置き場。指定ファイル名で置くと自動表示(画像の入れ方.txt参照)
```

## 絶対に守ること(変更禁止事項)

1. **1ファイル完結を維持する。** ビルドツール・フレームワーク・npm依存を導入しない。index.htmlをブラウザで開けば動く状態を保つ
2. **タグラインの表記を変えない**: 「知らないままの "これでいい" より、知った上での "これがいい" を。」(確定済みMVV)
3. **表現ルール(コンプライアンス)**: 「保証」「証明」という語を使わない / 「これが正解」等の断言をしない / ランキング形式にしない / 効果効能を謳わない(薬機法・景表法配慮)
4. **CTA先はLINE(lin.ee/6G6ifwV)のみ**。フォームや別の申込導線を追加しない(説明会は廃止済み)
5. **QRコードはbase64埋め込み済み**。触らない。LINE URLが変わる場合のみ再生成(python: qrcode ライブラリ)
6. **アクセシビリティの床**: prefers-reduced-motion対応・focus-visible・alt属性を壊さない
7. **Publicリポジトリ**。個人情報・未公開情報・許諾未確認の写真をコミットしない

## デザインシステム(:root変数で一括管理)

- パレット: cream #FDF7E8(背景) / caramel #C97F22 / terracotta #DD6F14 / rose #D88A8A / ink #46341F / LINE緑 #06C755(CTAボタン専用)
- フォント: 見出し=Zen Maru Gothic / 本文=Zen Kaku Gothic New(Google Fonts)
- シグネチャ要素: ヒーローの「"これでいい"に取り消し線→"これがいい"にマーカー」アニメーション。**このページの顔なので消さない**
- CTA三重化: ヒーロー直下 / スクロール追従バー(ヒーローCTA画面外で出現・最終CTAで消滅) / 最終CTAブロック

## 動作確認の方法

1. `python3 -m http.server 8000` → http://localhost:8000 で表示確認(file://直開きでも動くがフォントはネット必要)
2. スクリーンショットを撮って自分の目で確認する。**モバイル幅(375px)を最優先**、次にデスクトップ
3. チェック項目: タグラインアニメが動く / 追従バーの出現・消滅 / 写真プレースホルダ(点線枠)が画像投入で置き換わる / reduced-motion時に静止表示

## バックログ(優先度順)

### P1 — 公開直後にやる
- [ ] OGPメタタグ追加(og:title, og:description, og:image, twitter:card)。LINE・SlackでURL共有時の見え方が登録率に直結する。og:image用に images/ogp.jpg(1200×630)を参照する形にし、画像自体はデザインチームから受領
- [ ] favicon追加(協会ロゴ受領後。それまでは絵文字SVGファビコンで暫定可)
- [ ] 実写真の投入確認(images/画像の入れ方.txt のファイル名規約に従う)

### P2 — フィードバック反映
- [ ] 谷口CD・ママスタッフ5秒テストの指摘を反映(指摘はこのファイルの下に追記される想定)
- [ ] 「約750名」の実数確認結果を反映

### P3 — 計測
- [ ] GA4またはCloudflare Web Analytics導入(LINEボタンクリックをイベント計測)。ただしscriptは軽量に保つ
- [ ] LINE側の流入パラメータ運用(リンクを経路別に出し分けるか検討。園部さんと相談)

### P4 — 将来
- [ ] 独自ドメイン設定(community.maternity-food.org 等。CNAMEファイル追加+DNS設定)
- [ ] WordPress統合時の移植支援(全体を #andfamily-lp でスコープしCSS接頭辞化)。移行後は旧URLから301

## フィードバックメモ(レビュー後にここへ追記)

<!-- 例: 2026-08-01 谷口: ヒーローの余白をもう少し詰めたい -->
